% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/point_selection.R
\name{select_points_hall}
\alias{select_points_hall}
\title{Select Discriminative Points for Functional Data Classification}
\usage{
select_points_hall(
  data,
  grouping,
  method = "lda",
  cv = TRUE,
  p = 0.01,
  delta_t = 0.01,
  mean_error = "mgroups",
  parallel = FALSE
)
}
\arguments{
\item{data}{An object of class \code{fdata} or a matrix where each row is an observation.}

\item{grouping}{A factor specifying the class labels for each observation.}

\item{method}{Classification method to use. One of "lda", "qda", "NaiveBayes",
"logistic", "knn", "svm".}

\item{cv}{Logical. If TRUE, use cross-validation to estimate error.}

\item{p}{Numeric. Stopping criterion. Stop when improvement is less than p * error.}

\item{delta_t}{Numeric. Minimum distance between selected points (as a fraction of total points).}

\item{mean_error}{Metric for error. Either "mtotal" (overall error) or "mgroups" (mean error per group).}

\item{parallel}{Logical. If TRUE, use parallel processing (supported on Windows via \code{parLapply} or Unix via \code{mclapply}).}
}
\value{
A list containing:
  \itemize{
    \item \code{selected_indices}: Indices of the selected points.
    \item \code{error_path}: Vector of error rates at each iteration.
    \item \code{confusion_matrix}: Final confusion matrix using cross-validation.
  }
}
\description{
Implements the Hall & Bathia (2012) algorithm for selecting a subset of points
from functional data (e.g., wavelet coefficients or contours) that optimize
classification accuracy.
}
\examples{
\dontrun{
# Example usage with dummy data
library(fda.usc)
data(phoneme)
res <- select_points_hall(phoneme$data[1:100, ], phoneme$class[1:100], method = "lda")
}
}
